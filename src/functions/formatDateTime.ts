import type { FunctionMeta } from "@ruiapp/move-style";
import { formatDateTime } from "../utils/format-utility";

export default {
  name: "formatDateTime",
  func: formatDateTime,
} as FunctionMeta;