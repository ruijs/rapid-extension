import type { FunctionMeta } from "@ruiapp/move-style";
import { formatFileSize } from "../utils/format-utility";

export default {
  name: "formatFileSize",
  func: formatFileSize,
} as FunctionMeta;